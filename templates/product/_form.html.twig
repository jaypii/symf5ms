<div class="card bg-light p-2 mb-2">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</div>
